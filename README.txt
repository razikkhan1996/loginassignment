Its a node project so you have to install certain packages using npm.
MODULES NEEDED TO INSTALL

    "cookie-parser",
    "ejs",
    "express",
    "express-ejs-layouts"
    "mongoose"

The login page is runs on localhost port Number:8000

TRY THIS URL'S

1.in your browser type::localhost:8000-----this url simply opens the indexpage.

2.localhost:8000/users/sign-up

3.localhost:8000/users/sign-in